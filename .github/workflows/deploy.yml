name: Deploy site

on:
  push:
    branches:
      - master
      - main
    paths-ignore:
      - "README.md"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v4

      - name: Setup Ruby 💎
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: "3.3"
          bundler-cache: true

      - name: Install ImageMagick 🖼️
        run: sudo apt-get update && sudo apt-get install -y imagemagick

      - name: Setup Pages ⚙️
        id: pages
        uses: actions/configure-pages@v5

      - name: Build Site 🔧
        run: |
          export JEKYLL_ENV=production
          bundle exec jekyll build --baseurl "${{ steps.pages.base_path }}"

      - name: Upload Artifact 📦
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages 🚀
        id: deployment
        uses: actions/deploy-pages@v4
